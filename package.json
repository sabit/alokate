{
  "name": "alokate",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend && npm run build --workspace worker",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace worker",
    "test": "npm run test --workspace frontend",
    "format": "npm run format --workspace frontend"
  },
  "devDependencies": {
    "prettier": "3.2.5"
  }
}
